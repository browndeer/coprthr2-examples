This example demonstrates access to the host platform filesystem from the
coprocessor cores.

Note that the __ega() macro is used to convert local addresses into global
addresses within the UVA.  This can be eliminated if the conversion is done
on the host side.

$ make
$ ./main.x

